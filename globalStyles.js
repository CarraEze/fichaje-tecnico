import { StyleSheet } from "react-native"

export const globalStyles = StyleSheet.create({
    view: {
        flex: 1,
        alignItems: 'stretch',
        justifyContent: 'center',
        backgroundColor: "#000",
    }
});
